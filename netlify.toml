[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  # Disable smart detection for secrets scanning
  # The Gemini API key is intentionally exposed to client-side code (Vite requirement)
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"

